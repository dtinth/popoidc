# Public HTTPS issuer URL. Must exactly equal the `iss` claim and be reachable by
# relying parties (e.g. octo-sts). Traefik/Dokploy serves this domain.
POPOIDC_ISSUER=https://popoidc.example

# RSA private signing key, as a JWK with a "kid". Generate one with:
#   deno task keygen
# then paste the single-line JSON here (keep it secret).
POPOIDC_SIGNING_JWK={"kty":"RSA","n":"...","e":"AQAB","d":"...","p":"...","q":"...","dp":"...","dq":"...","qi":"...","kid":"2026-07-05","alg":"RS256","use":"sig"}

# Secret keying the stateless challenge HMAC. Use a long random string, e.g.
#   openssl rand -base64 48
POPOIDC_HMAC_SECRET=replace-with-a-long-random-secret

# Optional: pepper deriving HMAC-mode identities. Setting it ENABLES HMAC mode
# (POST /token with `secret` + `aud`); leaving it unset disables the mode.
# Generate with `openssl rand -base64 48`. Keep it separate from
# POPOIDC_HMAC_SECRET and NEVER rotate it: it names every shared-secret identity,
# so changing it renames all of them and breaks every trust policy using one.
# POPOIDC_HMAC_IDENTITY_SECRET=replace-with-a-long-random-secret

# Optional: SSHSIG namespace bound into Signing Proofs (default: popoidc)
# POPOIDC_NAMESPACE=popoidc

# Optional: listen port (default: 8000)
# PORT=8000
